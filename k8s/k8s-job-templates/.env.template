# RAG Embedder - Kubernetes Secret Template
# Copy this to .env and fill in your actual values
# Then create the secret with: kubectl create secret generic rag-embedder-env --from-file=.env

# Qdrant Configuration
QDRANT_HOST=192.168.58.1
QDRANT_PORT=6333

# Embedding Model Configuration
MODEL_NAME=Qwen/Qwen3-Embedding-8B

# API Token (REQUIRED - replace with your actual token)
API_TOKEN=your-api-token-here

# GitHub Token (optional, for private repos)
GITHUB_TOKEN=

# AWS Credentials (optional, for S3 uploads)
# If running on EKS with IRSA, these are not needed
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Debug Mode
DEBUG=false
