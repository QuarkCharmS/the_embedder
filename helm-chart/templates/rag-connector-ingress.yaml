{{- if and .Values.ragConnector.enabled .Values.ragConnector.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "rag-system.ragConnector.fullname" . }}
  labels:
    {{- include "rag-system.ragConnector.labels" . | nindent 4 }}
  {{- with .Values.ragConnector.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ragConnector.ingress.className }}
  ingressClassName: {{ .Values.ragConnector.ingress.className }}
  {{- end }}
  {{- if .Values.ragConnector.ingress.tls }}
  tls:
    {{- range .Values.ragConnector.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ragConnector.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "rag-system.ragConnector.fullname" $ }}
                port:
                  number: {{ $.Values.ragConnector.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
